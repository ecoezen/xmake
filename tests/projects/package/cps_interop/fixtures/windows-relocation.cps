{
  "cps_version": "0.13",
  "name": "winreloc",
  "version": "1.0.0",
  "prefix": "${prefix}",
  "components": {
    "winreloc": {
      "type": "archive",
      "includes": ["${prefix}/include"],
      "location": "${prefix}/lib/winreloc.lib"
    }
  }
}
